{
  "name": "demo-devops-agent",
  "version": "1.0.0",
  "description": "DevOps Agent demo: Lambda + DynamoDB throttling scenario",
  "bin": {
    "app": "bin/app.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --require-approval never",
    "destroy": "cdk destroy --force"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "~5.6.0",
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.0.0",
    "@aws-sdk/client-dynamodb": "^3.700.0"
  }
}
